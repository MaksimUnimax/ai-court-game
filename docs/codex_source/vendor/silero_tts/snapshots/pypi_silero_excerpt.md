# PyPI silero excerpt

Source URL: https://pypi.org/project/silero/
Captured at UTC: 2026-05-25T03:04:44Z
Retrieval method: web page text extraction from the public PyPI project page
Content type: extracted text from HTML

Relevant summary

- The package page describes `silero` as a small Python package around Silero TTS models.
- The page confirms the same three usage paths: PyTorch Hub, pip package, and manual caching/loading.
- The page includes the same TTS model/speaker tables and the Russian speaker coverage.
- The page also contains package metadata and release/download information for the Python package itself.

Notes

The PyPI page mixes package metadata with upstream project description; this snapshot keeps only the relevant TTS usage information.
